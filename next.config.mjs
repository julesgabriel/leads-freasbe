/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "api.microlink.io",
            'aceternity.com'
        ],
    },
};

export default nextConfig;
